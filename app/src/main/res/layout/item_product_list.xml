<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="100dp"
    android:orientation="horizontal"
    android:paddingBottom="5dp"
    android:paddingLeft="10dp"
    android:paddingRight="10dp"
    android:paddingTop="5dp"
    android:id="@+id/itemList">

    <TextView
        android:id="@+id/product_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/product_price"
        android:layout_toStartOf="@id/productImage"

        android:text="Name:"
        android:textSize="20dp" />

    <TextView
        android:id="@+id/product_price"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/product_chosen_amount"
        android:layout_gravity="left"
        android:layout_toStartOf="@id/productImage"
        android:text="Price:"
        android:textSize="20dp" />

    <ImageView
        android:id="@+id/productImage"
        android:layout_width="91dp"
        android:layout_height="87dp"
        android:src="@drawable/tnuva_milk3"
        android:layout_alignParentEnd="true"/>

    <TextView
        android:id="@+id/product_chosen_amount"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/productImage"
        android:layout_toStartOf="@id/productImage"
        android:text="Amount:"
        android:textSize="20dp" />


</RelativeLayout>